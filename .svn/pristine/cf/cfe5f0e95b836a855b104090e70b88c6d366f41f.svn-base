<!-- 所属情報入力 -->
@extends('menu.main')

@section('title', '所属情報入力')

@section('content')
    <div id="contents-stage">
        <table class="BaseContainerStyle1">
            <tbody>
                <tr>
                    <td>
                        <div id="UpdatePanel1">
                            {{--  <form action="" method="POST" id="form">
                                @csrf
                                <div class="FunctionMenu1">
                                    <input name="btnDisp" id="btnAdd" class="submit-form" type="button" value="新規所属登録"
                                        data-url="{{ route('MT23.update') }}">
                                </div>
                            </form>  --}}
                                <div class="line"></div>

                                <div class="GridViewStyle1">
                                    <table>
                                        <tbody>
                                            <tr>
                                                <th>
                                                    所属
                                                </th>
                                            </tr>
                                        </tbody>
                                    </table>
                                </div>
                                <div class="GridViewStyle1">
                                    <div class="two-col">
                                        <form action="" method="POST">
                                            {{ csrf_field() }}
                                            <table tabindex="7" class="GridViewSpace" id="gvEmp"
                                                style="border-collapse: collapse;" border="1" rules="all" cellspacing="0">
                                                <tbody>
                                                    @if (!empty($haken_company))
                                                        @foreach ($haken_company as $company)
                                                            <tr>
                                                                <td class="col-sm-4">
                                                                    <a href="{{ url('master/' . $company->COMPANY_CD . '/MT23CompanyEditor') }}"
                                                                        class="btn btn-light">
                                                                        {{ $company->COMPANY_CD }} :
                                                                        {{ $company->COMPANY_NAME }}
                                                                    </a>
                                                                </td>
                                                            </tr>
                                                        @endforeach
                                                    @endif
                                                </tbody>
                                            </table>
                                        </form>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-center">
                                    {{ $haken_company->appends(request()->input())->render('pagination::bootstrap-4') }}
                                </div>

                        </div>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
@endsection
@section('script')

    <script>
        $(document).on('click', '.submit-form', function() {
            var url = $(this).data('url');
            $('#form').attr('action', url);
            $('#form').submit();
        });

        //F5キーによるリロードを禁止
        document.addEventListener("keydown", function(e) {
            if ((e.which || e.keyCode) == 116) {
                e.preventDefault();
            }
        });
    </script>
@endsection
